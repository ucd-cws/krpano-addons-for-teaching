<!--Currently testing a button-->

<action name="testing" >
  hotspot.name;
</action>

<action name="whoahotspot">
  addhotspot("ZOMG NEW HOTSPOT");
</action>

<action name="draghotspot">
  if(dragging, 
	screentosphere(mouse.x,mouse.y,ath,atv);
	delayedcall(0,draghotspot());
	);
</action>

